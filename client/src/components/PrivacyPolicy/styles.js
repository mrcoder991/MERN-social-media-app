import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    PolicyContainer: {
        marginTop: '100px',
    },
    ContentPaper: {
        padding: '2em',
        marginBottom:'2em'  
    },
    List: {
        paddingLeft:'2em',
    }
}));